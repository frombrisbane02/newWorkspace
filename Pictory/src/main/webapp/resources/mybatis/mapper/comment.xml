<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="mybatis.mapper.comment">	
	<select id="commentList" parameterType="Map" resultType="commentDto">
		SELECT C.*, U.userId, U.userNickname
			FROM USERS U 
				INNER JOIN COMMENT_TBL C ON U.userNo=C.userNo
			WHERE postNo=#{postNo}
			ORDER BY C.cDate
	</select>
</mapper>