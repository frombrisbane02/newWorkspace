<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="mybatis.mapper.story">	

	<select id="virtuallist" parameterType="String" resultMap="map">
		SELECT u.userNo, s.sNo, ph.photoUrl
		FROM USERS u, PHOTO ph, STORY s
		WHERE u.userNo = s.userNo
		AND s.sNo = ph.sNo;
	</select>

</mapper>